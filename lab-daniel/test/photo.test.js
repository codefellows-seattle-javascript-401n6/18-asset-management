'use strict';

const agent = require('superagent');
const photo = require('../router/photoRoute');

describe('photoRoute()', () => {
    it('checks if the upload worked and a resource object is returned', (done) => {
        
    })

    it('returns all the resources that have been uploaded', (done) => {
        
    })

    it('returns info on one resource that\'s been uploaded', (done) => {
        
    })
})